apiVersion: v1
kind: Service
metadata:
  name: student-backend-service
spec:
  type: NodePort
  selector:
    app: student-backend
  ports:
    - protocol: TCP
      port: 3000      # service port
      targetPort: 3000
      nodePort: 32000  # external port on your machine
